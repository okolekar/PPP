#Trial
import numpy as np
import math as m
import Processed_Inputs as ip 

'''t = 0.1
s = 0.5
i = 0
for i in range(10):
        R = ip.ratioI*(2*np.sqrt(t/np.pi)*np.exp(-(s**2/(4*t)))-s*m.erfc(s/(2*np.sqrt(t))))+ip.Ta
        if np.abs(R) < np.exp(-5):
            break
        else:
            s += 0.1
else:
        raise ValueError("The NRS in the Initial Condition failed to converge in 10 steps")

print(f"The value obtained is for s is {s} at time t {t}")'''

